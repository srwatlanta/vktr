<h1><%= @genre.name%></h1>
<%= image_tag @genre.img_url%>
<p>Bio: <%= @genre.bio%></p>

<% @genre.videos.each do |video| %>
    <ul>
    <li><%= link_to video.title, video %>
    <%= link_to image_tag(video.img_url), video %></li>
    </ul>
<% end %>